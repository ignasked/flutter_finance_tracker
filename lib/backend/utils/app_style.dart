import 'package:flutter/material.dart';

class ColorPalette {
  static const Color primary = Color(0xFF669bbc);
  static const Color secondary = Color(0xFF003049);
  static const Color background = Color(0xFFfdf0d5);
  static const Color text = Color(0xFF151515);
  static const Color expense = Color(0xFFc1121f);
  static const Color income = Color(0xFF669bbc);
}

class AppStyle {
  static const List<Color> predefinedColors = [
    Colors.red,
    Colors.blue,
    Colors.green,
    Colors.orange,
    Colors.purple,
    Colors.yellow,
    Colors.brown,
    Colors.pink,
    Colors.teal,
    Colors.amber,
    Colors.grey,
    Colors.deepOrange,
    Colors.indigo,
  ];

  static const List<IconData> predefinedIcons = [
    Icons.category,
    Icons.fastfood,
    Icons.directions_car,
    Icons.movie,
    Icons.attach_money,
    Icons.local_hospital,
    Icons.lightbulb,
    Icons.restaurant,
    Icons.shopping_cart,
    Icons.home,
    Icons.miscellaneous_services,
    Icons.fitness_center,
    Icons.sports_esports,
    Icons.power,
    Icons.shopping_bag,
    Icons.recycling,
    Icons.card_giftcard,
    Icons.business_center,
    Icons.local_offer,
    Icons.more_horiz,
  ];
}
